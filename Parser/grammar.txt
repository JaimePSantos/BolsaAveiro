expr    : term ((PLUS|MINUS) term)*

term    : factor ((MUL|DIV) factor)*

factor  : INT|FLOAT
				: (PLUS|MINUS) factor
				: LPAREN expr RPAREN


intervalExpr : interval ( (++ | ** | ^-1) interval )?

interval : LOWERLIM INT SEPARATOR INT UPPERLIM
