Parser
    IMPROVE FORALL;
    define diamond;
    define subtraction/inverse interval;
    define inverse interval;
    program assignment only needs : and not := , right box only needs } and not }];
    Better way of representing powers.
    Division.

Translator
    Translate programs correctly.
    Figure out how to detect and translate parenthesis.
